package ch.ethz.computationgraph

class Dsl {
	/*
	Simulation, control, stepping through calculation

	input('name.as[String]) {
		(name) =>
		val message = f(name)
		output('message -> message)
	}
	aspirate 10ul A
	dispense 10ul B
	measure B
	if measurement is within 10%
	  success
	else if measurement is <90%
	  repeat with missing volume
	else
	  alert user
	
	
	*/
	
